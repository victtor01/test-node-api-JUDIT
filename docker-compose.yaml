version: '3.8'

services:

  postgres:
    image: postgres:alpine
    ports:
      - '5435:5432'
    container_name: 'postgres'
    restart: always
    volumes:
      - ./postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

volumes:
  postgres: